# Лекция 4 (05.03.26)
## Разбор домашних заданий 

### Задача 1. Расширенный код Хэмминга (8,4)
Порождающая матрица кода Хэмминга (7,4) получается из проверочной матрицы (приведение к виду $[P^T | I]$).  
Для расширения до (8,4) добавляем столбец, чтобы каждая строка имела чётный вес:  
$$
G_{8,4} = \begin{bmatrix}
I_4 & P \\
\hline
\mathbf{1}^T & \mathbf{1}
\end{bmatrix}
\quad \text{(последний столбец — все 1 для чётности)}.
$$
Перебор всех $2^4 = 16$ информационных слов → кодовые слова.  
Веса кодовых слов: 0, 4 или 8.  
**Минимальное расстояние** $d=4$ (наименьший ненулевой вес).  

Расстояние между любыми двумя кодовыми словами $c_i, c_j$ равно весу их суммы (линейность):  
$$
d(c_i, c_j) = \mathrm{wt}(c_i + c_j) \in \{0,4,8\}.
$$
Пары с расстоянием 8 перечислены явно; остальные — расстояние 4.

### Задача 2. Дуальный код к коду с проверкой на чётность
Код с проверкой на чётность: $n,n-1$, проверочная матрица — строка из единиц.  
Дуальный код: порождающая матрица становится проверочной и наоборот → новый код $(n,1)$.  
Информационных слов: $2^1 = 2$ (000…0 и 111…1).  
Кодовых слов: 2.  
Минимальное расстояние = вес ненулевого слова = $n$.  
Корректирующая способность:  
$$
t = \left\lfloor \frac{n-1}{2} \right\rfloor.
$$

### Задача 3. Линейный код (5,3)
Дана порождающая матрица $G$.  
Проверочная матрица $H$ найдена решением системы $G H^T = 0$ (выбраны базисные столбцы).  
Характеристики: $n=5$, $k=3$, $d=1$ (сумма строк $G$ даёт вектор веса 1).  
Скорость $R=3/5$, $t=0$ (не исправляет ошибки).  

**Таблица синдромного декодирования**:  
$s = r H^T$, синдромы $s_1,s_2$. Таблица неоднозначна (возможны перестановки позиций ошибок).

### Задача 4. Код Голея (бинарный)
Проверочная матрица (7×24).  
По теореме: любой набор из $d-1=6$ столбцов линейно независим; существует набор из 7 независимых столбцов → $d=7$.  
Число кодовых слов $M=2^{12}$.  
Проверяется равенство границе Хэмминга:  
$$
M = \frac{2^{24}}{\sum_{i=0}^{3} \binom{24}{i}} \implies \text{достигает границы Хэмминга}.
$$

(Код Хэмминга-симплекс перенесён на следующее занятие.)

## Классы эквивалентности (смежные классы кодового пространства)

Пусть $C = \{c_1, \dots, c_M\}$ — линейный код, $|C|=2^k$, пространство $\mathbb{F}_2^n$.  

**Смежный класс** (coset) с лидером $a$:  
$$
C + a = \{c + a \mid c \in C\}.
$$

- Если $a \in C$ — получаем тот же набор $C$ (с точностью до перестановки).  
- Если $a \notin C$ — новый класс, отличный от $C$.  

Всего различных смежных классов:  
$$
\frac{2^n}{2^k} = 2^{n-k}.
$$
Каждый класс содержит ровно $2^k$ векторов.  
Смежные классы разбивают всё пространство $\mathbb{F}_2^n$ на непересекающиеся подмножества.

## Декодирование по максимуму правдоподобия (ДПМП) для двоично-симметричного канала (ДСК)

**Модель ДСК**:  
$$
p(y_i \mid x_i) = 
\begin{cases}
1-p_0, & y_i = x_i, \\
p_0, & y_i \neq x_i.
\end{cases}
$$

Функция правдоподобия:  
$$
P(\vec{g} \mid \vec{c}_m) = \prod_{i=1}^n p(g_i \mid c_{m i}).
$$

**Логарифм функции правдоподобия**:  
$$
\Lambda_m(\vec{g}) = \sum_{i=1}^n \ln p(g_i \mid c_{m i}).
$$

**Отношение правдоподобия** (LLR):  
$$
L(g_j) = \ln \frac{P(g_j \mid 1)}{P(g_j \mid 0)} =
\begin{cases}
\ln \frac{1-p_0}{p_0}, & g_j=1, \\
\ln \frac{p_0}{1-p_0}, & g_j=0.
\end{cases}
$$
Обозначаем $\delta = L(g_j)$ (одинаково для всех позиций в ДСК при $p_0 < 1/2$).

**Вектор надёжности** (наклонности):  
$$
L(\vec{g}) = (L(g_1),\dots,L(g_n)).
$$

**Преобразование кодового слова**:  
$$
\gamma_{m i} = 2c_{m i} - 1 \quad (\text{0} \to -1,\ 1 \to +1).
$$

**Теорема** (ДСК):  
Декодирование по максимуму правдоподобия $\equiv$ поиск кодового слова $\vec{c}_m$, максимизирующего скалярное произведение  
$$
\Lambda_m(\vec{g}) \propto (\vec{\gamma}_m, L(\vec{g})).
$$

**Доказательство** (разность для двух кодовых слов $c_m$ и $c_m'$):  
Разбиваем позиции на четыре множества $I_{00}, I_{11}, I_{10}, I_{01}$.  
Разность скалярных произведений:  
$$
(\vec{\gamma}_m - \vec{\gamma}_{m'}, L(\vec{g})) = 2\sum_{j\in I_{10}} L(g_j) - 2\sum_{j\in I_{01}} L(g_j).
$$
После подстановки $L(g_j)$ и перегруппировки:  
$$
= 2(\Lambda_m(\vec{g}) - \Lambda_{m'}(\vec{g})).
$$
Отсюда: $\max (\vec{\gamma}_m, L) \iff \max \Lambda_m$.

**Эквивалентность минимуму расстояния Хэмминга** (ДСК, $p_0 < 1/2$):  
$$
(\vec{\gamma}_m, L(\vec{g})) = \delta \bigl( n - 2\, d_H(\vec{g}, \vec{c}_m) \bigr).
$$
Максимизация $\iff$ минимизация $d_H(\vec{g}, \vec{c}_m)$.  
Таким образом, ДПМП = декодирование по минимуму расстояния Хэмминга.

## Решётка и алгоритм Витерби

### Свойства решётки кода
- Вершины разбиты на **ярусы** (уровни) — непересекающиеся подмножества.  
- Каждый ярус содержит **ровно одну** вершину в начале и в конце (нулевой и финальный).  
- Граф **направленный** (только вперёд).  
- Рёбра соединяют **только соседние** ярусы.  
- Каждому ребру приписана **метрика** (вес = расстояние Хэмминга на данном символе).  
- Длина пути = сумма метрик рёбер.  
- Декодирование = поиск **кратчайшего** пути от стартовой вершины к финальной.

### Построение решётки (общий метод)
Разбиваем каждое кодовое слово $c_m = (c_m^p, c_m^f)$ на **прошлое** ($p$) и **будущее** ($f$) в точке $i$.  

$$
F_i(c^p) = \{ c^f \mid (c^p, c^f) \in C \}.
$$

**Правило соединения вершин** $v$ и $v'$ на соседних ярусах:  
существует хотя бы одно будущее, начинающееся с символа, переводящего $c^p$ из $v$ в $v'$.

### Пример: код (3,2) с порождающей матрицей
$$
G = \begin{pmatrix} 1 & 1 & 0 \\ 0 & 1 & 1 \end{pmatrix}.
$$
Кодовые слова:  
$$
C = \begin{pmatrix}
0 & 0 & 0 \\
1 & 1 & 0 \\
0 & 1 & 1 \\
1 & 0 & 1
\end{pmatrix}.
$$

Решётка (после схлопывания лишних узлов):  
- Ярусы: 0 → 1 → 2 → 3.  
- Профиль: (1, 2, 2, 1).  
Пример декодирования принятого вектора 1 0 0:  
- Метрики Хэмминга на рёбрах накапливаются.  
- В узлах слияния оставляем только путь с минимальной метрикой.  
- Финальные кандидаты: 0 0 0 и 1 0 1 (оба расстояние 1).

**Сложность**: определяется **профилем** решётки (число узлов на каждом ярусе). Чем меньше узлов — тем эффективнее декодер.

### Пример построения решётки для кода с 8 словами (продолжение в следующей лекции)
(Подробная табличная конструкция с past/future, $F_i$, $d(x_i)$, $v_i$ — см. оригинальные заметки страниц 7–8; алгоритм полностью разобран в лекции.)

**Домашнее задание**: подготовить конспект по решётке до конца примера (доделаем на следующем занятии).
