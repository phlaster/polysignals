# Лекция 8 (09.04.26)

## 0. Обсуждение домашних заданий

Разобрано **ДЗ по разложению** $x^7 - 1$ над $\mathrm{GF}(2)$:

- Получено **6** порождающих полиномов $g(x)$.
- Идентифицированы коды:
  - $1 + x + x^3$ → код Хэмминга $(7,4,3)$
  - $1 + x^2 + x^3$ → **тот же** код Хэмминга (другой примитивный полином)
  - $1 + x + x^2 + x^3$ → **дуальный код Хэмминга** (симплекс-код $(7,3,4)$)
  - $1 + x^4 + x^5 + x^6$ → **дуальный код Хэмминга**
  - $1 + x + x^3 + x^4$ → **дуальный код Хэмминга**
  - Полное произведение всех неприводимых → **код с повторением** $(7,1,7)$

**Таблица сводки** (пример из лекции):

| $g(x)$                  | Код                          | Параметры          |
|---------------------------|------------------------------|--------------------|
| $1+x+x^3$               | Хэмминга                     | $(7,4,3)$        |
| $1+x^2+x^3$             | Хэмминга                     | $(7,4,3)$        |
| $1+x+x^2+x^3$           | Дуальный Хэмминга (симплекс) | $(7,3,4)$        |
| …                         | …                            | …                  |
| Все множители             | С повторением                | $(7,1,7)$        |

**Вторая задача** (построение трёх решёток по новой матрице $G$ 3×6) переносится на следующее занятие.

**Конспекты**: дедлайн по 6-й лекции перенесён на завтра (09.04). Те, кто уже сдал — отметьте в чате.

## 1. Алгебраические структуры (повторение и углубление)

**Напоминание**:
- **Группа** — множество с одной операцией (замкнутость, ассоциативность, единица, обратный).
- **Кольцо** — сложение (абелева группа) + умножение (замкнутость, ассоциативность, дистрибутивность). Деление **не обязательно**.
- **Поле** — кольцо, в котором ненулевые элементы образуют **мультипликативную группу** (деление возможно).

**Мультипликативная группа поля $\mathrm{GF}(q)$**:  
Состоит из $q-1$ ненулевых элементов.  
**Теорема.** Мультипликативная группа любого конечного поля **циклическая**.

### Порядок элемента
Пусть $\alpha \in \mathrm{GF}(q)$.  
**Порядок** элемента $\alpha$ — наименьшее натуральное $s > 0$ такое, что  
$$
\alpha^s = 1.
$$

**Свойства**:
- $s$ делит $q-1$.
- Если порядки $s_A$ и $s_B$ **взаимно просты**, то  
  $$
  \mathrm{ord}(A \cdot B) = s_A \cdot s_B.
  $$

**Теорема** (существование элементов любого порядка).  
Если $s^t$ делит $q-1$, то в $\mathrm{GF}(q)$ **существует** элемент порядка $s^t$.

**Следствие (Ферма для конечных полей)**:  
Любой элемент $\alpha \in \mathrm{GF}(q)$ удовлетворяет  
$$
\alpha^{q-1} = 1 \quad \text{(ненулевой)}, \qquad \alpha^q = \alpha.
$$

**Пример**: $\mathrm{GF}(7)$, $q-1=6$, делители: 1, 2, 3, 6.  
Все ненулевые элементы имеют порядок, делящий 6.

**Примитивный элемент** ($\alpha$):  
Элемент **максимального порядка** $q-1$.  
Генерирует всю мультипликативную группу:  
$$
\{1, \alpha, \alpha^2, \dots, \alpha^{q-2}\}.
$$

## 2. Примитивные полиномы

**Определение.**  
Мономический неприводимый полином $p(x) \in \mathbb{F}_2[x]$ степени $m$ называется **примитивным**, если его корень $\alpha$ в поле $\mathrm{GF}(2^m)$ является **примитивным элементом** (порядок $\alpha = 2^m - 1$).

**Пример** (из ДЗ):  
$p(x) = 1 + x + x^3$ — примитивный над $\mathrm{GF}(2)$.  
Элементы $\mathrm{GF}(8)$:  
$$
0,\ 1,\ \alpha,\ \alpha^2,\ \alpha^3 = \alpha+1,\ \alpha^4 = \alpha^2+\alpha,\ \alpha^5 = \alpha^2+\alpha+1,\ \alpha^6 = \alpha^2+1.
$$

**Характеристика поля** $p$:  
Наименьшее число $p$ такое, что $1 + 1 + \dots + 1$ ($p$ раз) = 0.  
Для всех $\mathrm{GF}(2^m)$ характеристика равна **2**.

## 3. Минимальные полиномы

**Определение.**  
**Минимальный полином** $m_\alpha(x)$ элемента $\alpha \in \mathrm{GF}(2^m)$ над $\mathrm{GF}(2)$ — это приведённый полином **наименьшей степени**, для которого $\alpha$ является корнем.

**Свойства**:
- $m_\alpha(x)$ **неприводим**.
- Любой полином с корнем $\alpha$ делится на $m_\alpha(x)$.
- $\deg m_\alpha(x) \leq m$; для **примитивного** элемента $\deg m_\alpha(x) = m$.

## 4. Циклические коды Хэмминга через примитивные полиномы

Код Хэмминга $(2^m-1, 2^m-m-1, 3)$ — **циклический**.  
Порождающий полином $g(x) =$ **примитивный** полином степени $m$.

**Проверочная матрица** $H$ (самый удобный вид):  
Столбцы — все ненулевые элементы поля в виде векторов (степени примитивного $\alpha$):

$$
H = \bigl[ 1 \;\; \alpha \;\; \alpha^2 \;\; \dots \;\; \alpha^{2^m-2} \bigr].
$$

**Пример** ($m=3$):  
$g(x) = 1 + x + x^3$, $\alpha^3 = \alpha + 1$.  
Получаем классическую $H$ кода Хэмминга $(7,4)$.

## 5. Коды максимальной длины (дуальные коды Хэмминга, симплекс-коды)

Если в качестве **порождающего** полинома взять **примитивный** полином, а в качестве **проверочного** — тоже примитивный, получаем **дуальный** код Хэмминга — **код максимальной длины** (simplex-код).

Параметры: $(2^m-1, m, 2^{m-1})$.  
Все ненулевые кодовые слова имеют **одинаковый вес** $2^{m-1}$.

**Реализация кодирования** — **регистр сдвига с линейной обратной связью (LFSR)**:

- $m$ ячеек (регистр).
- Обратные связи определяются коэффициентами примитивного полинома.
- При начальном заполнении (не все нули) регистр проходит **все** $2^m - 1$ ненулевых состояний → на выходе последовательность **максимальной длины**.

**Пример** ($m=4$, $n=15$):  
Примитивный полином $1 + x + x^4$.  
Схема LFSR с 4 ячейками и обратными связями по $x^4$ и $x$.

**Домашнее задание** (продолжение примера):  
Продолжить симуляцию LFSR (начальное состояние 0010) и получить полную последовательность длины 15. Убедиться, что все 15 ненулевых состояний пройдены.

**Вопросы к следующей лекции**:  
- Коды БЧХ и Рида–Соломона (корни порождающего полинома).  
- Декодирование по минимальному расстоянию в LFSR-кодах.
